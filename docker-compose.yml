
services:
  backend:
    build:
      context: ./backend
    container_name: spotify-back
    ports:
      - "5100:5100"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped

  frontend:
    build:
      context: ./Spotify
    container_name: spotify-front
    ports:
      - "5500:5500"
    volumes:
      - ./Spotify:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    depends_on:
      - backend
    restart: unless-stopped
